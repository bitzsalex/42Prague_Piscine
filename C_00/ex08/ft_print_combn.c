#include <unistd.h>

void ft_print_combn(int n);

void ft_print_combn(int n)
{
    int lastDigit = 10 - n;
    int outer = 0;
    int digitCombs[n];  // norm error
    int finalPosition;
    int currentPosition;

    // loop until we arri
    while (outer <= lastDigit)
    {
        outer++;
    }
}